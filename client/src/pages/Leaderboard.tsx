import AcademyHeader from "@/components/AcademyHeader";
import { trpc } from "@/lib/trpc";
import { Award, Trophy } from "lucide-react";

export default function Leaderboard() {
  const { data: leaderboard = [], isLoading } = trpc.academy.getLeaderboard.useQuery();
  return <div className="min-h-screen bg-[#fbfcf7]"><AcademyHeader /><main className="container py-14 sm:py-20"><div className="mx-auto max-w-3xl text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-[#e9f5d0] text-[#245b5b]"><Trophy className="h-6 w-6" /></div><div className="mt-5 font-mono-brand text-xs uppercase tracking-[0.18em] text-[#2c7e7c]">Campaign referrals</div><h1 className="mt-4 font-display text-5xl leading-[1] tracking-[-0.05em] text-[#163c3e] sm:text-6xl">The verified <em className="text-[#2c7e7c]">leaderboard.</em></h1><p className="mx-auto mt-5 max-w-xl leading-7 text-[#607a7c]">Positions are ranked by qualified referrals only. Public results show safe student names and referral totals — nothing private.</p></div><div className="mx-auto mt-12 max-w-3xl overflow-hidden rounded-3xl border border-[#d5e2d6] bg-white academy-shadow"><div className="grid grid-cols-[70px_1fr_auto] gap-3 border-b border-[#e0ebe1] bg-[#f2f7ef] px-5 py-4 text-xs font-semibold uppercase tracking-[0.12em] text-[#638184]"><span>Rank</span><span>Student</span><span>Qualified</span></div>{isLoading ? <div className="p-8 text-center text-[#607a7c]">Loading verified rankings…</div> : leaderboard.length ? leaderboard.map((entry) => <div key={entry.rank} className="grid grid-cols-[70px_1fr_auto] items-center gap-3 border-b border-[#edf2ed] px-5 py-5 last:border-0"><div className={`grid h-10 w-10 place-items-center rounded-xl font-mono-brand text-sm ${entry.rank <= 3 ? "bg-[#d4f36b] text-[#123a3c]" : "bg-[#eef4ed] text-[#567779]"}`}>{String(entry.rank).padStart(2, "0")}</div><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#123a3c] text-sm font-semibold text-white">{entry.name.charAt(0)}</span><span className="font-semibold text-[#163c3e]">{entry.name}</span></div><span className="text-sm font-semibold text-[#286665]">{entry.referrals}</span></div>) : <div className="p-10 text-center"><Award className="mx-auto h-7 w-7 text-[#9aaead]" /><h2 className="mt-4 text-lg font-semibold">The first qualified referrals will appear here.</h2><p className="mt-2 text-sm leading-6 text-[#607a7c]">Students receive a personal share link after they register for the campaign.</p></div>}</div></main></div>;
}
